# ADR-015 — Event outbox для timeline и уведомлений

Статус: Принято (реализация PHASE-04+) · Дата: 2026-07-08

Доменные модули публикуют события в outbox-таблицу в той же транзакции; воркер доставляет в timeline/notifications. Согласованность без распределённых транзакций.
