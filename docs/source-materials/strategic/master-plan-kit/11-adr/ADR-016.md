# ADR-016 — Background jobs: PostgreSQL-очередь (SKIP LOCKED) → ARQ+Redis

Статус: Принято · Дата: 2026-07-08

Старт без Redis: таблица jobs + FOR UPDATE SKIP LOCKED + systemd-воркер. Переход на ARQ+Redis при Oura-sync и заметном OCR-потоке (PHASE-07). Интерфейс постановки задач абстрагирован.
