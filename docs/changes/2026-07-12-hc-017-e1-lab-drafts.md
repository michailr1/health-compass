# 2026-07-12 — HC-017 E1 Source-preserving Lab Drafts

HC-017 Slice E1 was implemented through PR `#61` and merged into repository `main`.

Verified implementation head:

```text
419386e909207ab67921c008e210c059aba6658c
```

Merge commit:

```text
2ad0ca47d994472201c218b3e6af37145cbacdec
```

CI run:

```text
#477 — success
```

Repository Alembic head:

```text
0057
```

E1 adds:

- owner/edit-only source-preserving Lab drafts;
- exact reviewed OCR candidate/source-role provenance;
- explicit numeric, text and qualitative value kinds;
- explicit unknown/not-present source decisions;
- current document, finalized OCR review and patient-decision checks;
- active consent checks on every mutation;
- optimistic concurrency;
- FORCE RLS and restricted mutation functions;
- content-free audit;
- minimal Lab draft API and UI.

E1 does not add:

- confirmed Lab observations;
- automatic canonical mapping;
- unit conversion;
- reference-range interpretation;
- medical diagnosis or recommendations;
- metric dynamics;
- production deployment.

Production remains:

```text
application: b8e868825f378195975e2729f3f36c21a1afa2d0
Alembic: 0049
DOCUMENT_UPLOAD_ENABLED=false
```

Next stage:

```text
HC-017 Slice E2 — Explicit Confirmation and Confirmed Observations
```

E2 requires a separate security review, immutable observation/source snapshots and an explicit atomic idempotent confirmation transaction.
