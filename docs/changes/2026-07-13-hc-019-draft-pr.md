# 2026-07-13 — HC-019 draft implementation PR opened

## Status

```text
HC-019 IMPLEMENTED IN DRAFT PR / CI REQUIRED / NOT MERGED / NOT DEPLOYED
PR: #71
branch: feat/hc-019-navigation-empty-state
code head before documentation: 326b2a6b4537de7139398925eec27a8c23f4ca46
```

## Implemented scope

- primary navigation is `Главная · История · Добавить · Ассистент · Ещё` on mobile and desktop;
- active navigation uses `aria-current="page"` and tested route rules;
- implementation/demo tabs leave primary navigation;
- secondary routes move under `Ещё`;
- user-facing `Документы` becomes `Анализы`;
- exact upload, review and confirmation explanation is applied;
- storage-path and quarantine product language is removed;
- disabled upload hides file selection and upload actions;
- enabled upload action is `Загрузить`;
- empty-dashboard CTA follows live upload capability and otherwise opens the health profile;
- `Подключить источник` is removed while no real integration exists;
- `Сон` replaces vendor-shaped `Oura` navigation while `/app/oura` remains redirect-compatible;
- demo history, source cards, Oura charts and hard-coded dashboard KPI values are not presented as profile data;
- legacy demo/mock dashboard snapshots are detected and hidden;
- assistant availability is represented honestly without a fake conversation.

## Verification added

Frontend tests cover:

- exact five-item navigation;
- active-state route handling;
- exact approved Analyses and secure-storage copy;
- capability-aware dashboard actions;
- live capability request ordering and failure handling;
- demo/mock source detection;
- user-facing document, scanner, rendering and OCR labels.

## Boundaries

No backend code, PostgreSQL migration, RLS policy, document feature flag or worker configuration changed.

Production remains unchanged:

```text
application: fb1e7a2f70c4b24edbdff6dfd2889c34a63e2c75
Alembic: 0058
DOCUMENT_UPLOAD_ENABLED=false
scanner/renderer/reconciler/OCR workers: not running
HC-017 E3: not deployed
HC-019: not deployed
```

The draft PR must remain unmerged until exact-head CI and review complete. No production rollout is authorized by this record.
